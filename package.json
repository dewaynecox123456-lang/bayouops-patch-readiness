{
  "name": "bayouops-patch-readiness",
  "version": "0.1.0-EarlyAccess",
  "private": true,
  "description": "Local-first operational readiness intelligence tooling for infrastructure teams.",
  "type": "module",
  "scripts": {
    "start": "./scripts/bayouops-start.sh",
    "serve": "./scripts/serve.sh",
    "test": "bash -n scripts/*.sh && find tools -name '*.mjs' -print0 | xargs -0 -n1 node --check && ./scripts/release-smoke-test.sh",
    "build-release": "./scripts/build-release.sh"
  }
}
